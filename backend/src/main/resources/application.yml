spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/chaoxing?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8}
    driverClassName: ${DB_DRIVER:com.mysql.cj.jdbc.Driver}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}
  sql:
    init:
      mode: ${SPRING_SQL_INIT_MODE:never}
      schema-locations: classpath:schema.sql
      data-locations: classpath:seed.sql
  mail:
    host: smtp.qq.com
    port: 587
    username: ${SPRING_MAIL_USERNAME:1763690540@qq.com}
    password: ${SPRING_MAIL_PASSWORD:junyntmffvxadjce}
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

mybatis:
  mapper-locations: classpath*:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

server:
  port: 8083

security:
  token:
    secret: change-me-dev
  cors:
    allowed-origins: http://localhost:5173,http://localhost:8080

# 人脸识别（防作弊）配置
baidu:
  face:
    api-key: ${BAIDU_FACE_API_KEY:}
    secret-key: ${BAIDU_FACE_SECRET_KEY:}
    threshold: ${BAIDU_FACE_THRESHOLD:80.0}

# AI辅助解题配置（DeepSeek）
ai:
  deepseek:
    api-key: ${AI_DEEPSEEK_API_KEY:dummy-key}
    api-url: ${AI_DEEPSEEK_API_URL:https://api.deepseek.com/chat/completions}
    model: ${AI_DEEPSEEK_MODEL:deepseek-chat}
